# Python Kanban Board

A simple Kanban board project written in Python.  
Currently, this is just the initial setup with a `main()` function that prints a welcome message.  
The goal is to build a task management system step by step.

